sequenceDiagram
    participant n0 as Scheduled agent controls
    participant n1 as Schedule API
    participant n2 as Schedule and occurrence records
    Note over n0: Start#58; Validate and submit the schedule editor. #91;Confirmed#93;
    n0->>n1: Check enabled capabilities and validate the schedule workspace. #124; Create Agent submits POST /api/schedules after draft validation. #91;Confirmed#93;
    n1->>n2: Normalize recurrence and persist the definition with its next run time. #124; Validate and persist the schedule including recurrence and optional workflow. #91;Confirmed#93;
    n2->>n1: Ensure the agent has a dedicated conversation#59; remove the definition if that cannot be created. #124; Return the saved definition so the API can ensure its dedicated chat. #91;Confirmed#93;
    n1->>n0: Update the schedule list, close the editor and refresh the sidebar’s session metadata. #124; Return the schedule#59; update its UI list, close the editor and refresh chat metadata. #91;Confirmed#93;
